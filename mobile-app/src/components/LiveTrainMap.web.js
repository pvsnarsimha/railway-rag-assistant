// Web: the website's Live Tracking draws its own Leaflet map inline (see
// the web-only map code in screens/LiveTrackingScreen.js), so nothing here.
export default function LiveTrainMap() {
  return null;
}
